# Preserve results from successful sources

A Partial Refresh Run will preserve and may notify Articles returned by successful sources while reporting the degraded sources. When every configured source fails, Paper Monitor preserves all existing Article Listings, records a failed Refresh Run, and sends no notifications; a successful run with no new Articles is not a failure.
