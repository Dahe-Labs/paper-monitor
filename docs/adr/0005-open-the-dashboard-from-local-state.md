# Open the dashboard from local state

Opening the Paper Monitor dashboard will read Article Listings and Refresh Run status from local SQLite without starting or waiting for a network refresh. Successfully rendered listings become Presented Articles, while an empty first-run dashboard shows an explicit refresh action rather than performing hidden network work.
